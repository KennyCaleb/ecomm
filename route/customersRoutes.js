const express = require("express")
const router = express.Router()
const {} = 
  
router.post("/", )
router.post("/login");
router.get("/");
router.get("/:id");
router.put("/:id");
router.delete("/:id");

module.exports = router